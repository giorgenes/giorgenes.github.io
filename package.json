{
  "scripts": {
    "dev": "webpack serve --mode development --allowed-hosts .gitpod.io",
    "start": "npm run dev",
    "unit": "jest --config test/unit/jest.conf.js --coverage",
    "lint": "eslint --ext .js src test/unit",
    "build": "node build.js > dist/index.hml"
  },
  "dependencies": {
    "pug": "^3.0.2",
    "webpack": "^5.58.2",
    "webpack-cli": "^4.9.0"
  },
  "devDependencies": {
    "webpack-dev-server": "^4.3.1"
  }
}